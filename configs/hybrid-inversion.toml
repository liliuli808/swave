[hybrid]
dataset_config = "configs/dataset.toml"
dataset_dir = "data/production"
forward_checkpoint = "runs/production-48g/best.pt"
supervised_dir = "runs/supervised-inversion-v2"
output_dir = "results/hybrid-inversion"
mode_weights = [4.0, 1.0, 1.0, 1.0]
smoothness_lambda = 0.01
regularization_type = "adaptive"
prior_lambda_candidates = [0.0001, 0.001, 0.01, 0.1, 1.0]
sensitivity_epsilon_fraction = 0.01
sensitivity_phase_floor = 0.000000000001
prior_weight_min = 0.25
prior_weight_max = 4.0
vs_min = 0.3
vs_max = 2.6
reference_width = 0.7
max_iterations = 100
relative_tolerance = 0.00001
validation_samples_per_kind = 100
noise_scenarios = ["clean", "noise_1pct"]
seed = 20260727
device = "auto"
workers = 0
threads_per_worker = 1
task_index = 0
task_count = 1
